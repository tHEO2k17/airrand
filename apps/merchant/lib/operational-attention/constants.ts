/** Orders created within this window show a "New" badge. */
export const NEW_ORDER_BADGE_MS = 5 * 60 * 1000;

/** Polling refresh only alerts for orders newer than this (avoids stale poll diffs). */
export const POLL_NEW_ORDER_MAX_AGE_MS = 2 * 60 * 1000;

export const MUTE_PREFERENCE_KEY = "airrand_merchant_alerts_muted";

export const PROCESSED_EVENT_IDS_MAX = 200;
